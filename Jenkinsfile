import jenkins.model.*;
import hudson.plugins.ec2.*;
import groovy.json.*

pipeline {
    agent any
    stages {
        stage('Sample') {
            steps {
                script {
                    echo "Muneer"
                }
            }
        }
    }
}
